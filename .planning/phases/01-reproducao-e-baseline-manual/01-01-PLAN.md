---
phase: 01-reproducao-e-baseline-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md
autonomous: false
requirements:
  - REPR-01
  - REPR-02
  - REPR-03
requirements_addressed:
  - REPR-01
  - REPR-02
  - REPR-03
must_haves:
  truths:
    - "O executor consegue confirmar que Docker, rAthena, MariaDB e Korangar estao prontos para o fluxo local."
    - "O executor reproduz o fluxo real `@kill -> Respawn` antes de qualquer fix permanente."
    - "O resultado visual final fica registrado em baseline manual, incluindo pose do player e estado da janela Respawn."
  artifacts:
    - path: ".planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md"
      provides: "Registro manual de ambiente, comandos, passos e resultado visual observado"
      contains: "Resultado Observado"
  key_links:
    - from: "play.bat"
      to: "korangar/target/release/korangar.exe"
      via: "launcher com CWD correto para assets"
      pattern: "play.bat"
    - from: "docker-compose.yml"
      to: "rAthena login/char/map + MariaDB"
      via: "docker compose up -d login char map"
      pattern: "PACKETVER.*20220406"
---

<objective>
Criar a baseline manual da Phase 1 para provar o estado atual do bug de respawn antes de qualquer alteracao permanente.

Purpose: separar falha de ambiente de falha de gameplay e gerar evidencia comparavel para as fases seguintes.
Output: `.planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md` com ambiente, comandos, conta/personagem, passos `@kill -> Respawn` e resultado visual observado.

Este plano cobre REPR-01, REPR-02 e REPR-03. Ele nao deve corrigir codigo, nao deve adicionar instrumentacao, nao deve instalar framework de teste e nao deve alterar Korangar, rAthena, Docker, assets ou `play.bat`.
</objective>

<execution_context>
@C:/Users/jhonata/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jhonata/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reproducao-e-baseline-manual/01-RESEARCH.md
@.planning/phases/01-reproducao-e-baseline-manual/01-VALIDATION.md
@.planning/phases/01-reproducao-e-baseline-manual/01-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@AGENTS.md

<interfaces>
Fronteiras operacionais relevantes para o executor:

- `docker compose up -d login char map` usa a stack local existente e deve manter `PACKETVER=20220406`.
- `docker compose ps` e `Test-NetConnection` sao smoke checks suficientes para REPR-01.
- `.\play.bat` e o launcher obrigatorio porque ajusta o CWD para `korangar/korangar/` e valida assets locais.
- `@kill -> Respawn` deve ser executado no cliente real. HP/movimento sozinhos nao provam sucesso; registrar pose visual e janela Respawn separadamente.
- Credenciais/portas sao dados de laboratorio local, nao configuracao de producao.
</interfaces>
</context>

<source_audit>
## Cobertura de Fontes

| Fonte | Item | Cobertura |
|-------|------|-----------|
| GOAL | Desenvolvedor reproduz e documenta o bug atual localmente antes de fix permanente | Task 1 prepara o ambiente, Task 2 reproduz o fluxo e Task 3 grava baseline |
| REQ | REPR-01 ambiente local inicia o suficiente | Task 1 |
| REQ | REPR-02 bug atual reproduzido com `@kill -> Respawn` | Task 2 |
| REQ | REPR-03 roteiro registra passos, ambiente, comando e resultado visual | Task 3 |
| RESEARCH | Usar `play.bat`; manter `PACKETVER=20220406`; nao criar E2E; nao diagnosticar pacotes na Phase 1 | Tasks 1-3 |
| CONTEXT | Sem `01-CONTEXT.md`; decisoes bloqueadas vem de AGENTS/PROJECT/STATE | Tasks 1-3 respeitam pt-BR, fix minimo comprovado e nenhuma edicao de codigo |

Itens diferidos mantidos fora do plano: E2E automatizado, tracing permanente, refatoracao de `main.rs`, patch preventivo em rAthena, mudanca de `PACKETVER`.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar ambiente local reproduzivel</name>
  <files>.planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md</files>
  <action>
  Criar ou sobrescrever `.planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md` com o template recomendado em `01-PATTERNS.md`. Preencher a secao inicial de ambiente com data/hora, git/status relevante sem reverter nada, Windows cwd, `PACKETVER=20220406`, launcher `.\play.bat`, executavel Korangar, GRFs e status dos servicos.

  Executar somente comandos nao destrutivos a partir de `D:\ragnarok`: `docker compose up -d login char map`, `docker compose ps`, `docker compose logs --no-color --tail=80 login char map`, `Test-NetConnection` para portas 6900, 6121 e 5121, e `Test-Path` para `.\play.bat`, `.\korangar\target\release\korangar.exe`, `.\korangar\korangar\data.grf` e `.\korangar\korangar\rdata.grf`. Nao executar `docker compose down -v`, nao editar `docker-compose.yml`, nao rebuildar Korangar/rAthena e nao alterar fontes.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'D:\ragnarok'; docker compose ps; Test-Path '.\play.bat'; Test-Path '.\korangar\target\release\korangar.exe'; Test-Path '.\korangar\korangar\data.grf'; Test-Path '.\korangar\korangar\rdata.grf'"</automated>
    <manual>Conferir em `01-BASELINE.md` que ambiente, comandos executados e qualquer falha de readiness foram registrados sem esconder bloqueios.</manual>
  </verify>
  <acceptance_criteria>REPR-01 fica coberto somente quando os servicos necessarios, portas, launcher, executavel e assets estao confirmados como prontos. Se qualquer item essencial falhar, registrar o bloqueio em `01-BASELINE.md`, marcar a fase como bloqueada e nao considerar REPR-01 coberto.</acceptance_criteria>
  <done>`01-BASELINE.md` existe com secao Ambiente preenchida e smoke operacional registrado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reproduzir `@kill -> Respawn` no cliente real</name>
  <files>.planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md</files>
  <action>
  Abrir o cliente usando `.\play.bat` a partir de `D:\ragnarok`. Usar conta GM local documentada, preferencialmente `admin` / `123` se disponivel, selecionar um personagem e entrar no mapa. No chat, executar `@kill`, confirmar que a janela de Respawn aparece, clicar Respawn e observar o estado final.

  Registrar imediatamente em `01-BASELINE.md`: conta e personagem usados, comando executado, se o player ficou visualmente em pe/vivo ou deitado/morto, se a janela Respawn fechou ou permaneceu aberta, se HP foi restaurado, se movimento funcionou e se o bug foi reproduzido. Nao usar `@alive` como substituto do clique Respawn para esta baseline; `@alive` pode ser citado apenas como observacao separada se usado acidentalmente.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'D:\ragnarok'; Test-Path '.\play.bat'; docker compose ps"</automated>
    <manual>Fluxo observado no jogo: login -> mapa -> `@kill` -> janela Respawn -> clique Respawn -> registro do estado visual final, janela, HP e movimento.</manual>
  </verify>
  <acceptance_criteria>REPR-02 fica coberto somente quando o fluxo `@kill -> Respawn` foi executado no cliente real antes de fix permanente e o bug atual foi observado. Se a execucao manual nao puder acontecer, ou se o bug nao reproduzir no binario atual, registrar a evidencia em `01-BASELINE.md`, marcar a fase como bloqueada/escalada e nao considerar REPR-02 coberto.</acceptance_criteria>
  <done>`01-BASELINE.md` contem o resultado observado do fluxo manual e separa pose visual, janela Respawn, HP e movimento.</done>
</task>

<task type="auto">
  <name>Task 3: Fechar baseline manual e checar escopo</name>
  <files>.planning/phases/01-reproducao-e-baseline-manual/01-BASELINE.md</files>
  <action>
  Revisar `01-BASELINE.md` para garantir que ele contem: objetivo, ambiente, comandos executados, fluxo manual numerado, resultado observado, evidencias disponiveis, conclusao e mapeamento explicito para REPR-01/REPR-02/REPR-03. Se houver screenshot/video manual, registrar apenas o caminho ou descricao; nao mover assets grandes nem criar nova infraestrutura.

  Conferir que nenhum arquivo de codigo ou configuracao foi alterado como parte desta fase. Se `git status` mostrar mudancas preexistentes fora da baseline, registra-las como preexistentes/nao tocadas no resumo em vez de reverter. Criar o summary exigido pelo workflow em `.planning/phases/01-reproducao-e-baseline-manual/01-01-SUMMARY.md` somente se o executor GSD exigir; caso contrario, limitar a entrega a `01-BASELINE.md`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'D:\ragnarok'; Test-Path '.\.planning\phases\01-reproducao-e-baseline-manual\01-BASELINE.md'; Select-String -Path '.\.planning\phases\01-reproducao-e-baseline-manual\01-BASELINE.md' -Pattern 'Ambiente','Comandos Executados','Fluxo Manual','Resultado Observado','Conclusao'"</automated>
    <manual>Confirmar que a baseline responde: qual ambiente foi usado, qual comando matou o personagem, qual acao de Respawn foi feita e qual estado visual foi observado.</manual>
  </verify>
  <acceptance_criteria>REPR-03 fica coberto quando o roteiro de reproducao registra passos, ambiente, comando usado e resultado visual observado em um artefato unico.</acceptance_criteria>
  <done>`01-BASELINE.md` esta completo, sem mudancas permanentes em Korangar/rAthena/Docker/launcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host Windows -> Docker services | Portas MariaDB/login/char/map publicadas no host para laboratorio local |
| Cliente Korangar -> rAthena local | Login e comandos GM trafegam em stack local de desenvolvimento |
| Executor -> working tree | Comandos de baseline podem afetar arquivos se o escopo nao for controlado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `docker-compose.yml`, portas 3306/6900/6121/5121 e credenciais dev | mitigate | Tratar portas e credenciais como local-only no baseline; nao publicar como producao; nao alterar binds/config durante Phase 1 |
| T-01-02 | Elevation of Privilege | Conta GM local e comandos `@kill`/Respawn | accept | Aceito por ser laboratorio local necessario para reproducao; registrar conta/personagem usados e nao transformar esse perfil em recomendacao de producao |
| T-01-03 | Tampering | Korangar, rAthena, Docker e launcher | mitigate | Plano permite modificar somente `01-BASELINE.md`; comandos destrutivos e edicoes de fonte/config estao proibidos |
| T-01-04 | Denial of Service | Stack Docker local | mitigate | Usar `docker compose up -d` e smoke checks; nao usar `down -v`, limpar volumes ou resetar banco durante baseline |
| T-01-05 | Repudiation | Resultado visual manual | mitigate | Registrar data/hora, comandos, conta/personagem, criterios separados e evidencia textual/screenshot se disponivel |

Nao ha ameaca de alta severidade que exija bloquear a fase quando o ambiente permanece local-only e o plano nao altera codigo nem configuracao. Se o executor detectar bind publico, credenciais reais ou comando destrutivo necessario, deve pausar e pedir decisao humana.
</threat_model>

<verification>
Verificacao geral da fase:

1. `docker compose ps` mostra os servicos necessarios para login/char/map prontos; se nao mostrar, a fase fica bloqueada.
2. `.\play.bat` foi usado como launcher do cliente, preservando CWD correto.
3. O fluxo manual `@kill -> Respawn` foi tentado no cliente real antes de fix permanente.
4. `01-BASELINE.md` registra ambiente, comandos, conta/personagem, passos, pose visual final, janela Respawn, HP, movimento e conclusao.
5. Nenhum arquivo de Korangar, rAthena, Docker, imports, assets ou launcher foi modificado pela fase.

Comando smoke final:

```powershell
powershell -NoProfile -Command "Set-Location 'D:\ragnarok'; Test-Path '.\.planning\phases\01-reproducao-e-baseline-manual\01-BASELINE.md'; docker compose ps"
```
</verification>

<success_criteria>
- REPR-01: ambiente local necessario para Korangar, rAthena, Docker e MariaDB confirmado como pronto; bloqueio documentado nao conta como sucesso.
- REPR-02: fluxo `@kill -> Respawn` executado no cliente real antes de qualquer fix permanente, com o bug atual observado; tentativa bloqueada ou nao-reproducao nao conta como sucesso e deve escalar a fase.
- REPR-03: roteiro de reproducao salvo em `01-BASELINE.md` com passos, ambiente, comando usado e resultado visual observado.
- Escopo: nenhuma correcao de codigo, nenhuma instrumentacao nova, nenhum framework de teste novo e `PACKETVER=20220406` preservado.
</success_criteria>

<output>
Depois da execucao, criar `.planning/phases/01-reproducao-e-baseline-manual/01-01-SUMMARY.md` conforme o workflow GSD, incluindo requisitos cobertos, comandos smoke executados e caminho da baseline.
</output>
